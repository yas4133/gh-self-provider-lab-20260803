name: GH-R60 nested outer provider
description: Provider composite resolving provider-owned nested action

outputs:
  marker:
    description: Nested inner resolution marker
    value: ${{ steps.inner.outputs.marker }}

runs:
  using: composite
  steps:
    - id: inner
      uses: $/actions/nested-inner
